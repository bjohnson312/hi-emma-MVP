// Code generated by the Encore 1.50.6 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/
import type { CookieWithOptions } from "encore.dev/api";

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the  Encore application.
 */
export class Client {
    public readonly admin_auth: admin_auth.ServiceClient
    public readonly admin_portal: admin_portal.ServiceClient
    public readonly auth: auth.ServiceClient
    public readonly care_team: care_team.ServiceClient
    public readonly conversation: conversation.ServiceClient
    public readonly journal: journal.ServiceClient
    public readonly journey: journey.ServiceClient
    public readonly morning: morning.ServiceClient
    public readonly notifications: notifications.ServiceClient
    public readonly patient_sharing: patient_sharing.ServiceClient
    public readonly profile: profile.ServiceClient
    public readonly provider_auth: provider_auth.ServiceClient
    public readonly provider_portal: provider_portal.ServiceClient
    public readonly voice: voice.ServiceClient
    public readonly wellness: wellness.ServiceClient
    public readonly wellness_journal: wellness_journal.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.admin_auth = new admin_auth.ServiceClient(base)
        this.admin_portal = new admin_portal.ServiceClient(base)
        this.auth = new auth.ServiceClient(base)
        this.care_team = new care_team.ServiceClient(base)
        this.conversation = new conversation.ServiceClient(base)
        this.journal = new journal.ServiceClient(base)
        this.journey = new journey.ServiceClient(base)
        this.morning = new morning.ServiceClient(base)
        this.notifications = new notifications.ServiceClient(base)
        this.patient_sharing = new patient_sharing.ServiceClient(base)
        this.profile = new profile.ServiceClient(base)
        this.provider_auth = new provider_auth.ServiceClient(base)
        this.provider_portal = new provider_portal.ServiceClient(base)
        this.voice = new voice.ServiceClient(base)
        this.wellness = new wellness.ServiceClient(base)
        this.wellness_journal = new wellness_journal.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * Import the auth handler to be able to derive the auth type
 */
import type { auth as auth_auth } from "~backend/auth/clerk";

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    /**
     * Allows you to set the authentication data to be used for each
     * request either by passing in a static object or by passing in
     * a function which returns a new object for each request.
     */
    auth?: RequestType<typeof auth_auth> | AuthDataGenerator
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { login as api_admin_auth_login_login } from "~backend/admin_auth/login";

export namespace admin_auth {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.login = this.login.bind(this)
        }

        public async login(params: RequestType<typeof api_admin_auth_login_login>): Promise<ResponseType<typeof api_admin_auth_login_login>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/auth/login`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_auth_login_login>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { deactivateUser as api_admin_portal_deactivate_user_deactivateUser } from "~backend/admin_portal/deactivate_user";
import { exportUsers as api_admin_portal_export_users_exportUsers } from "~backend/admin_portal/export_users";
import { getAccessStats as api_admin_portal_get_access_stats_getAccessStats } from "~backend/admin_portal/get_access_stats";
import { getSystemInfo as api_admin_portal_get_system_info_getSystemInfo } from "~backend/admin_portal/get_system_info";
import { getUsageStats as api_admin_portal_get_usage_stats_getUsageStats } from "~backend/admin_portal/get_usage_stats";
import { listUsers as api_admin_portal_list_users_listUsers } from "~backend/admin_portal/list_users";
import { logAccess as api_admin_portal_log_access_logAccess } from "~backend/admin_portal/log_access";
import { resetPassword as api_admin_portal_reset_password_resetPassword } from "~backend/admin_portal/reset_password";

export namespace admin_portal {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.deactivateUser = this.deactivateUser.bind(this)
            this.exportUsers = this.exportUsers.bind(this)
            this.getAccessStats = this.getAccessStats.bind(this)
            this.getSystemInfo = this.getSystemInfo.bind(this)
            this.getUsageStats = this.getUsageStats.bind(this)
            this.listUsers = this.listUsers.bind(this)
            this.logAccess = this.logAccess.bind(this)
            this.resetPassword = this.resetPassword.bind(this)
        }

        public async deactivateUser(params: RequestType<typeof api_admin_portal_deactivate_user_deactivateUser>): Promise<ResponseType<typeof api_admin_portal_deactivate_user_deactivateUser>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/users/deactivate`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_deactivate_user_deactivateUser>
        }

        public async exportUsers(): Promise<ResponseType<typeof api_admin_portal_export_users_exportUsers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/users/export`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_export_users_exportUsers>
        }

        public async getAccessStats(): Promise<ResponseType<typeof api_admin_portal_get_access_stats_getAccessStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/access-stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_get_access_stats_getAccessStats>
        }

        public async getSystemInfo(): Promise<ResponseType<typeof api_admin_portal_get_system_info_getSystemInfo>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/system-info`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_get_system_info_getSystemInfo>
        }

        public async getUsageStats(): Promise<ResponseType<typeof api_admin_portal_get_usage_stats_getUsageStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/usage-stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_get_usage_stats_getUsageStats>
        }

        public async listUsers(): Promise<ResponseType<typeof api_admin_portal_list_users_listUsers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/users`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_list_users_listUsers>
        }

        public async logAccess(params: RequestType<typeof api_admin_portal_log_access_logAccess>): Promise<ResponseType<typeof api_admin_portal_log_access_logAccess>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/log-access`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_log_access_logAccess>
        }

        public async resetPassword(params: RequestType<typeof api_admin_portal_reset_password_resetPassword>): Promise<ResponseType<typeof api_admin_portal_reset_password_resetPassword>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/users/reset-password`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_portal_reset_password_resetPassword>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { login as api_auth_login_login } from "~backend/auth/login";
import { signup as api_auth_signup_signup } from "~backend/auth/signup";

export namespace auth {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.login = this.login.bind(this)
            this.signup = this.signup.bind(this)
        }

        public async login(params: RequestType<typeof api_auth_login_login>): Promise<ResponseType<typeof api_auth_login_login>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/login`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_login_login>
        }

        public async signup(params: RequestType<typeof api_auth_signup_signup>): Promise<ResponseType<typeof api_auth_signup_signup>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/auth/signup`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_auth_signup_signup>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { addMember as api_care_team_add_member_addMember } from "~backend/care_team/add_member";
import { deleteMember as api_care_team_delete_member_deleteMember } from "~backend/care_team/delete_member";
import { getMembersNeedingEmail as api_care_team_get_members_needing_email_getMembersNeedingEmail } from "~backend/care_team/get_members_needing_email";
import { getSetupProgress as api_care_team_get_setup_progress_getSetupProgress } from "~backend/care_team/get_setup_progress";
import { listMembers as api_care_team_list_members_listMembers } from "~backend/care_team/list_members";
import { updateMember as api_care_team_update_member_updateMember } from "~backend/care_team/update_member";
import { updateSetupProgress as api_care_team_update_setup_progress_updateSetupProgress } from "~backend/care_team/update_setup_progress";

export namespace care_team {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addMember = this.addMember.bind(this)
            this.deleteMember = this.deleteMember.bind(this)
            this.getMembersNeedingEmail = this.getMembersNeedingEmail.bind(this)
            this.getSetupProgress = this.getSetupProgress.bind(this)
            this.listMembers = this.listMembers.bind(this)
            this.updateMember = this.updateMember.bind(this)
            this.updateSetupProgress = this.updateSetupProgress.bind(this)
        }

        public async addMember(params: RequestType<typeof api_care_team_add_member_addMember>): Promise<ResponseType<typeof api_care_team_add_member_addMember>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/care-team/members`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_care_team_add_member_addMember>
        }

        public async deleteMember(params: RequestType<typeof api_care_team_delete_member_deleteMember>): Promise<ResponseType<typeof api_care_team_delete_member_deleteMember>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                userId: params.userId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/care-team/members/${encodeURIComponent(params.id)}`, {query, method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_care_team_delete_member_deleteMember>
        }

        public async getMembersNeedingEmail(params: RequestType<typeof api_care_team_get_members_needing_email_getMembersNeedingEmail>): Promise<ResponseType<typeof api_care_team_get_members_needing_email_getMembersNeedingEmail>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                userId: params.userId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/care-team/members-needing-email`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_care_team_get_members_needing_email_getMembersNeedingEmail>
        }

        public async getSetupProgress(params: { userId: string }): Promise<ResponseType<typeof api_care_team_get_setup_progress_getSetupProgress>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/care-team/setup-progress/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_care_team_get_setup_progress_getSetupProgress>
        }

        public async listMembers(params: RequestType<typeof api_care_team_list_members_listMembers>): Promise<ResponseType<typeof api_care_team_list_members_listMembers>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                activeOnly: params.activeOnly === undefined ? undefined : String(params.activeOnly),
                userId:     params.userId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/care-team/members`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_care_team_list_members_listMembers>
        }

        public async updateMember(params: RequestType<typeof api_care_team_update_member_updateMember>): Promise<ResponseType<typeof api_care_team_update_member_updateMember>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                address:      params.address,
                email:        params.email,
                fax:          params.fax,
                isPrimary:    params.isPrimary,
                name:         params.name,
                notes:        params.notes,
                organization: params.organization,
                phone:        params.phone,
                relationship: params.relationship,
                specialty:    params.specialty,
                userId:       params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/care-team/members/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_care_team_update_member_updateMember>
        }

        public async updateSetupProgress(params: RequestType<typeof api_care_team_update_setup_progress_updateSetupProgress>): Promise<ResponseType<typeof api_care_team_update_setup_progress_updateSetupProgress>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                currentStep:    params.currentStep,
                isCompleted:    params.isCompleted,
                stepsCompleted: params.stepsCompleted,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/care-team/setup-progress/${encodeURIComponent(params.userId)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_care_team_update_setup_progress_updateSetupProgress>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { chat as api_conversation_chat_chat } from "~backend/conversation/chat";
import { getMemorySummary as api_conversation_get_memories_getMemorySummary } from "~backend/conversation/get_memories";
import {
    getConversationByDate as api_conversation_history_getConversationByDate,
    getPastConversations as api_conversation_history_getPastConversations
} from "~backend/conversation/history";
import {
    getMemories as api_conversation_memory_getMemories,
    storeMemory as api_conversation_memory_storeMemory
} from "~backend/conversation/memory";
import {
    completeSession as api_conversation_session_completeSession,
    getOrCreateSession as api_conversation_session_getOrCreateSession,
    updateSessionContext as api_conversation_session_updateSessionContext
} from "~backend/conversation/session";

export namespace conversation {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.chat = this.chat.bind(this)
            this.completeSession = this.completeSession.bind(this)
            this.getConversationByDate = this.getConversationByDate.bind(this)
            this.getMemories = this.getMemories.bind(this)
            this.getMemorySummary = this.getMemorySummary.bind(this)
            this.getOrCreateSession = this.getOrCreateSession.bind(this)
            this.getPastConversations = this.getPastConversations.bind(this)
            this.storeMemory = this.storeMemory.bind(this)
            this.updateSessionContext = this.updateSessionContext.bind(this)
        }

        public async chat(params: RequestType<typeof api_conversation_chat_chat>): Promise<ResponseType<typeof api_conversation_chat_chat>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/chat`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_chat_chat>
        }

        public async completeSession(params: { session_id: number }): Promise<ResponseType<typeof api_conversation_session_completeSession>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/session/${encodeURIComponent(params.session_id)}/complete`, {method: "PUT", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_session_completeSession>
        }

        public async getConversationByDate(params: RequestType<typeof api_conversation_history_getConversationByDate>): Promise<ResponseType<typeof api_conversation_history_getConversationByDate>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/by-date`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_history_getConversationByDate>
        }

        public async getMemories(params: RequestType<typeof api_conversation_memory_getMemories>): Promise<ResponseType<typeof api_conversation_memory_getMemories>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/memory/get`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_memory_getMemories>
        }

        public async getMemorySummary(params: RequestType<typeof api_conversation_get_memories_getMemorySummary>): Promise<ResponseType<typeof api_conversation_get_memories_getMemorySummary>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/memories`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_get_memories_getMemorySummary>
        }

        public async getOrCreateSession(params: RequestType<typeof api_conversation_session_getOrCreateSession>): Promise<ResponseType<typeof api_conversation_session_getOrCreateSession>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/session`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_session_getOrCreateSession>
        }

        public async getPastConversations(params: RequestType<typeof api_conversation_history_getPastConversations>): Promise<ResponseType<typeof api_conversation_history_getPastConversations>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/history`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_history_getPastConversations>
        }

        public async storeMemory(params: RequestType<typeof api_conversation_memory_storeMemory>): Promise<ResponseType<typeof api_conversation_memory_storeMemory>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/memory`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_memory_storeMemory>
        }

        public async updateSessionContext(params: RequestType<typeof api_conversation_session_updateSessionContext>): Promise<ResponseType<typeof api_conversation_session_updateSessionContext>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                context:        params.context,
                "current_step": params["current_step"],
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/session/${encodeURIComponent(params.session_id)}/context`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_conversation_session_updateSessionContext>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { accessShare as api_journal_access_share_accessShare } from "~backend/journal/access_share";
import { createShare as api_journal_create_share_createShare } from "~backend/journal/create_share";
import { exportJournal as api_journal_export_exportJournal } from "~backend/journal/export";
import { generateExport as api_journal_generate_export_generateExport } from "~backend/journal/generate_export";
import { generatePDF as api_journal_generate_pdf_generatePDF } from "~backend/journal/generate_pdf";
import { listShares as api_journal_list_shares_listShares } from "~backend/journal/list_shares";
import { revokeShare as api_journal_revoke_share_revokeShare } from "~backend/journal/revoke_share";

export namespace journal {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.accessShare = this.accessShare.bind(this)
            this.createShare = this.createShare.bind(this)
            this.exportJournal = this.exportJournal.bind(this)
            this.generateExport = this.generateExport.bind(this)
            this.generatePDF = this.generatePDF.bind(this)
            this.listShares = this.listShares.bind(this)
            this.revokeShare = this.revokeShare.bind(this)
        }

        public async accessShare(params: RequestType<typeof api_journal_access_share_accessShare>): Promise<ResponseType<typeof api_journal_access_share_accessShare>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journal/access-share`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journal_access_share_accessShare>
        }

        public async createShare(params: RequestType<typeof api_journal_create_share_createShare>): Promise<ResponseType<typeof api_journal_create_share_createShare>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journal/create-share`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journal_create_share_createShare>
        }

        public async exportJournal(params: RequestType<typeof api_journal_export_exportJournal>): Promise<ResponseType<typeof api_journal_export_exportJournal>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journal/export`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journal_export_exportJournal>
        }

        public async generateExport(params: RequestType<typeof api_journal_generate_export_generateExport>): Promise<ResponseType<typeof api_journal_generate_export_generateExport>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journal/generate-export`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journal_generate_export_generateExport>
        }

        public async generatePDF(params: RequestType<typeof api_journal_generate_pdf_generatePDF>): Promise<ResponseType<typeof api_journal_generate_pdf_generatePDF>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journal/generate-pdf`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journal_generate_pdf_generatePDF>
        }

        public async listShares(params: RequestType<typeof api_journal_list_shares_listShares>): Promise<ResponseType<typeof api_journal_list_shares_listShares>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journal/list-shares`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journal_list_shares_listShares>
        }

        public async revokeShare(params: RequestType<typeof api_journal_revoke_share_revokeShare>): Promise<ResponseType<typeof api_journal_revoke_share_revokeShare>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journal/revoke-share`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journal_revoke_share_revokeShare>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { getMilestones as api_journey_get_milestones_getMilestones } from "~backend/journey/get_milestones";
import { getJourneySetup as api_journey_get_setup_getJourneySetup } from "~backend/journey/get_setup";
import { updateJourneySetup as api_journey_update_setup_updateJourneySetup } from "~backend/journey/update_setup";

export namespace journey {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.getJourneySetup = this.getJourneySetup.bind(this)
            this.getMilestones = this.getMilestones.bind(this)
            this.updateJourneySetup = this.updateJourneySetup.bind(this)
        }

        public async getJourneySetup(params: RequestType<typeof api_journey_get_setup_getJourneySetup>): Promise<ResponseType<typeof api_journey_get_setup_getJourneySetup>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journey/setup/get`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journey_get_setup_getJourneySetup>
        }

        public async getMilestones(params: RequestType<typeof api_journey_get_milestones_getMilestones>): Promise<ResponseType<typeof api_journey_get_milestones_getMilestones>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journey/milestones/list`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journey_get_milestones_getMilestones>
        }

        public async updateJourneySetup(params: RequestType<typeof api_journey_update_setup_updateJourneySetup>): Promise<ResponseType<typeof api_journey_update_setup_updateJourneySetup>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/journey/setup/update`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_journey_update_setup_updateJourneySetup>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { addActivity as api_morning_add_activity_addActivity } from "~backend/morning/add_activity";
import { addJournalEntry as api_morning_add_journal_entry_addJournalEntry } from "~backend/morning/add_journal_entry";
import { checkIn as api_morning_check_in_checkIn } from "~backend/morning/check_in";
import { createRoutinePreference as api_morning_create_routine_preference_createRoutinePreference } from "~backend/morning/create_routine_preference";
import { getJournalEntries as api_morning_get_journal_entries_getJournalEntries } from "~backend/morning/get_journal_entries";
import { getRoutineHistory as api_morning_get_routine_history_getRoutineHistory } from "~backend/morning/get_routine_history";
import { getRoutinePreference as api_morning_get_routine_preference_getRoutinePreference } from "~backend/morning/get_routine_preference";
import { getRoutineStats as api_morning_get_routine_stats_getRoutineStats } from "~backend/morning/get_routine_stats";
import { getRoutineTemplates as api_morning_get_routine_templates_getRoutineTemplates } from "~backend/morning/get_routine_templates";
import { getMorningSetupProgress as api_morning_get_setup_progress_getMorningSetupProgress } from "~backend/morning/get_setup_progress";
import { getTodayCompletion as api_morning_get_today_completion_getTodayCompletion } from "~backend/morning/get_today_completion";
import { logRoutineCompletion as api_morning_log_routine_completion_logRoutineCompletion } from "~backend/morning/log_routine_completion";

export namespace morning {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addActivity = this.addActivity.bind(this)
            this.addJournalEntry = this.addJournalEntry.bind(this)
            this.checkIn = this.checkIn.bind(this)
            this.createRoutinePreference = this.createRoutinePreference.bind(this)
            this.getJournalEntries = this.getJournalEntries.bind(this)
            this.getMorningSetupProgress = this.getMorningSetupProgress.bind(this)
            this.getRoutineHistory = this.getRoutineHistory.bind(this)
            this.getRoutinePreference = this.getRoutinePreference.bind(this)
            this.getRoutineStats = this.getRoutineStats.bind(this)
            this.getRoutineTemplates = this.getRoutineTemplates.bind(this)
            this.getTodayCompletion = this.getTodayCompletion.bind(this)
            this.logRoutineCompletion = this.logRoutineCompletion.bind(this)
        }

        public async addActivity(params: RequestType<typeof api_morning_add_activity_addActivity>): Promise<ResponseType<typeof api_morning_add_activity_addActivity>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/activity/add`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_add_activity_addActivity>
        }

        public async addJournalEntry(params: RequestType<typeof api_morning_add_journal_entry_addJournalEntry>): Promise<ResponseType<typeof api_morning_add_journal_entry_addJournalEntry>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/journal/add`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_add_journal_entry_addJournalEntry>
        }

        public async checkIn(params: RequestType<typeof api_morning_check_in_checkIn>): Promise<ResponseType<typeof api_morning_check_in_checkIn>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_check_in_checkIn>
        }

        public async createRoutinePreference(params: RequestType<typeof api_morning_create_routine_preference_createRoutinePreference>): Promise<ResponseType<typeof api_morning_create_routine_preference_createRoutinePreference>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/preference/create`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_create_routine_preference_createRoutinePreference>
        }

        public async getJournalEntries(params: RequestType<typeof api_morning_get_journal_entries_getJournalEntries>): Promise<ResponseType<typeof api_morning_get_journal_entries_getJournalEntries>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/journal/get`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_get_journal_entries_getJournalEntries>
        }

        public async getMorningSetupProgress(params: RequestType<typeof api_morning_get_setup_progress_getMorningSetupProgress>): Promise<ResponseType<typeof api_morning_get_setup_progress_getMorningSetupProgress>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/setup/progress`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_get_setup_progress_getMorningSetupProgress>
        }

        public async getRoutineHistory(params: RequestType<typeof api_morning_get_routine_history_getRoutineHistory>): Promise<ResponseType<typeof api_morning_get_routine_history_getRoutineHistory>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"] === undefined ? undefined : params["end_date"].toISOString(),
                limit:        params.limit === undefined ? undefined : String(params.limit),
                "start_date": params["start_date"] === undefined ? undefined : params["start_date"].toISOString(),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/history/${encodeURIComponent(params.user_id)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_get_routine_history_getRoutineHistory>
        }

        public async getRoutinePreference(params: RequestType<typeof api_morning_get_routine_preference_getRoutinePreference>): Promise<ResponseType<typeof api_morning_get_routine_preference_getRoutinePreference>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/preference/get`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_get_routine_preference_getRoutinePreference>
        }

        public async getRoutineStats(params: RequestType<typeof api_morning_get_routine_stats_getRoutineStats>): Promise<ResponseType<typeof api_morning_get_routine_stats_getRoutineStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/stats`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_get_routine_stats_getRoutineStats>
        }

        public async getRoutineTemplates(): Promise<ResponseType<typeof api_morning_get_routine_templates_getRoutineTemplates>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/templates`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_get_routine_templates_getRoutineTemplates>
        }

        public async getTodayCompletion(params: { user_id: string }): Promise<ResponseType<typeof api_morning_get_today_completion_getTodayCompletion>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/today/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_get_today_completion_getTodayCompletion>
        }

        public async logRoutineCompletion(params: RequestType<typeof api_morning_log_routine_completion_logRoutineCompletion>): Promise<ResponseType<typeof api_morning_log_routine_completion_logRoutineCompletion>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/morning_routine/completion/log`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_morning_log_routine_completion_logRoutineCompletion>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    getPreferences as api_notifications_preferences_getPreferences,
    registerPushSubscription as api_notifications_preferences_registerPushSubscription,
    updatePreferences as api_notifications_preferences_updatePreferences
} from "~backend/notifications/preferences";
import {
    getPendingNotifications as api_notifications_send_getPendingNotifications,
    markNotificationRead as api_notifications_send_markNotificationRead,
    sendNotification as api_notifications_send_sendNotification
} from "~backend/notifications/send";

export namespace notifications {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.getPendingNotifications = this.getPendingNotifications.bind(this)
            this.getPreferences = this.getPreferences.bind(this)
            this.markNotificationRead = this.markNotificationRead.bind(this)
            this.registerPushSubscription = this.registerPushSubscription.bind(this)
            this.sendNotification = this.sendNotification.bind(this)
            this.updatePreferences = this.updatePreferences.bind(this)
        }

        public async getPendingNotifications(params: { user_id: string }): Promise<ResponseType<typeof api_notifications_send_getPendingNotifications>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/notifications/pending/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_notifications_send_getPendingNotifications>
        }

        public async getPreferences(params: { user_id: string }): Promise<ResponseType<typeof api_notifications_preferences_getPreferences>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/notifications/preferences/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_notifications_preferences_getPreferences>
        }

        public async markNotificationRead(params: RequestType<typeof api_notifications_send_markNotificationRead>): Promise<ResponseType<typeof api_notifications_send_markNotificationRead>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/notifications/mark-read`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_notifications_send_markNotificationRead>
        }

        public async registerPushSubscription(params: RequestType<typeof api_notifications_preferences_registerPushSubscription>): Promise<ResponseType<typeof api_notifications_preferences_registerPushSubscription>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/notifications/push-subscription`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_notifications_preferences_registerPushSubscription>
        }

        public async sendNotification(params: RequestType<typeof api_notifications_send_sendNotification>): Promise<ResponseType<typeof api_notifications_send_sendNotification>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/notifications/send`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_notifications_send_sendNotification>
        }

        public async updatePreferences(params: RequestType<typeof api_notifications_preferences_updatePreferences>): Promise<ResponseType<typeof api_notifications_preferences_updatePreferences>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/notifications/preferences`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_notifications_preferences_updatePreferences>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { getProviderNotes as api_patient_sharing_get_provider_notes_getProviderNotes } from "~backend/patient_sharing/get_provider_notes";
import { grantProviderAccess as api_patient_sharing_grant_provider_access_grantProviderAccess } from "~backend/patient_sharing/grant_provider_access";
import { listProviderAccess as api_patient_sharing_list_provider_access_listProviderAccess } from "~backend/patient_sharing/list_provider_access";
import { revokeProviderAccess as api_patient_sharing_revoke_provider_access_revokeProviderAccess } from "~backend/patient_sharing/revoke_provider_access";

export namespace patient_sharing {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.getProviderNotes = this.getProviderNotes.bind(this)
            this.grantProviderAccess = this.grantProviderAccess.bind(this)
            this.listProviderAccess = this.listProviderAccess.bind(this)
            this.revokeProviderAccess = this.revokeProviderAccess.bind(this)
        }

        public async getProviderNotes(params: { userId: string }): Promise<ResponseType<typeof api_patient_sharing_get_provider_notes_getProviderNotes>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patient/provider-notes/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_patient_sharing_get_provider_notes_getProviderNotes>
        }

        public async grantProviderAccess(params: RequestType<typeof api_patient_sharing_grant_provider_access_grantProviderAccess>): Promise<ResponseType<typeof api_patient_sharing_grant_provider_access_grantProviderAccess>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patient/grant-access`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_patient_sharing_grant_provider_access_grantProviderAccess>
        }

        public async listProviderAccess(params: { userId: string }): Promise<ResponseType<typeof api_patient_sharing_list_provider_access_listProviderAccess>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patient/provider-access/${encodeURIComponent(params.userId)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_patient_sharing_list_provider_access_listProviderAccess>
        }

        public async revokeProviderAccess(params: RequestType<typeof api_patient_sharing_revoke_provider_access_revokeProviderAccess>): Promise<ResponseType<typeof api_patient_sharing_revoke_provider_access_revokeProviderAccess>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                userId: params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/patient/revoke-access/${encodeURIComponent(params.providerId)}`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_patient_sharing_revoke_provider_access_revokeProviderAccess>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { acknowledgeInsight as api_profile_acknowledge_insight_acknowledgeInsight } from "~backend/profile/acknowledge_insight";
import { create as api_profile_create_create } from "~backend/profile/create";
import { get as api_profile_get_get } from "~backend/profile/get";
import { getConversationHistory as api_profile_get_conversation_history_getConversationHistory } from "~backend/profile/get_conversation_history";
import { getInsights as api_profile_get_insights_getInsights } from "~backend/profile/get_insights";
import { getMilestones as api_profile_get_milestones_getMilestones } from "~backend/profile/get_milestones";
import { logConversation as api_profile_log_conversation_logConversation } from "~backend/profile/log_conversation";
import { getRecommendations as api_profile_recommendations_getRecommendations } from "~backend/profile/recommendations";
import { update as api_profile_update_update } from "~backend/profile/update";
import { updateMorningPreferences as api_profile_update_morning_preferences_updateMorningPreferences } from "~backend/profile/update_morning_preferences";

export namespace profile {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.acknowledgeInsight = this.acknowledgeInsight.bind(this)
            this.create = this.create.bind(this)
            this.get = this.get.bind(this)
            this.getConversationHistory = this.getConversationHistory.bind(this)
            this.getInsights = this.getInsights.bind(this)
            this.getMilestones = this.getMilestones.bind(this)
            this.getRecommendations = this.getRecommendations.bind(this)
            this.logConversation = this.logConversation.bind(this)
            this.update = this.update.bind(this)
            this.updateMorningPreferences = this.updateMorningPreferences.bind(this)
        }

        public async acknowledgeInsight(params: RequestType<typeof api_profile_acknowledge_insight_acknowledgeInsight>): Promise<ResponseType<typeof api_profile_acknowledge_insight_acknowledgeInsight>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/insights/acknowledge`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_acknowledge_insight_acknowledgeInsight>
        }

        public async create(params: RequestType<typeof api_profile_create_create>): Promise<ResponseType<typeof api_profile_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_create_create>
        }

        public async get(params: { user_id: string }): Promise<ResponseType<typeof api_profile_get_get>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_get_get>
        }

        public async getConversationHistory(params: RequestType<typeof api_profile_get_conversation_history_getConversationHistory>): Promise<ResponseType<typeof api_profile_get_conversation_history_getConversationHistory>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "conversation_type": params["conversation_type"],
                limit:               params.limit === undefined ? undefined : String(params.limit),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/history/${encodeURIComponent(params.user_id)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_get_conversation_history_getConversationHistory>
        }

        public async getInsights(params: RequestType<typeof api_profile_get_insights_getInsights>): Promise<ResponseType<typeof api_profile_get_insights_getInsights>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/insights`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_get_insights_getInsights>
        }

        public async getMilestones(params: RequestType<typeof api_profile_get_milestones_getMilestones>): Promise<ResponseType<typeof api_profile_get_milestones_getMilestones>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/milestones`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_get_milestones_getMilestones>
        }

        public async getRecommendations(params: RequestType<typeof api_profile_recommendations_getRecommendations>): Promise<ResponseType<typeof api_profile_recommendations_getRecommendations>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/recommendations`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_recommendations_getRecommendations>
        }

        public async logConversation(params: RequestType<typeof api_profile_log_conversation_logConversation>): Promise<ResponseType<typeof api_profile_log_conversation_logConversation>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/conversation/log`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_log_conversation_logConversation>
        }

        public async update(params: RequestType<typeof api_profile_update_update>): Promise<ResponseType<typeof api_profile_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                name: params.name,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/${encodeURIComponent(params.user_id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_update_update>
        }

        public async updateMorningPreferences(params: RequestType<typeof api_profile_update_morning_preferences_updateMorningPreferences>): Promise<ResponseType<typeof api_profile_update_morning_preferences_updateMorningPreferences>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/morning-preferences`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_update_morning_preferences_updateMorningPreferences>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { login as api_provider_auth_login_login } from "~backend/provider_auth/login";
import { signup as api_provider_auth_signup_signup } from "~backend/provider_auth/signup";

export namespace provider_auth {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.login = this.login.bind(this)
            this.signup = this.signup.bind(this)
        }

        public async login(params: RequestType<typeof api_provider_auth_login_login>): Promise<ResponseType<typeof api_provider_auth_login_login>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/login`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_auth_login_login>
        }

        public async signup(params: RequestType<typeof api_provider_auth_signup_signup>): Promise<ResponseType<typeof api_provider_auth_signup_signup>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/signup`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_auth_signup_signup>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { addNote as api_provider_portal_add_note_addNote } from "~backend/provider_portal/add_note";
import { getAuditLogs as api_provider_portal_get_audit_logs_getAuditLogs } from "~backend/provider_portal/get_audit_logs";
import { getMessages as api_provider_portal_get_messages_getMessages } from "~backend/provider_portal/get_messages";
import { getNotes as api_provider_portal_get_notes_getNotes } from "~backend/provider_portal/get_notes";
import { getPatientData as api_provider_portal_get_patient_data_getPatientData } from "~backend/provider_portal/get_patient_data";
import { grantAccess as api_provider_portal_grant_access_grantAccess } from "~backend/provider_portal/grant_access";
import { listPatients as api_provider_portal_list_patients_listPatients } from "~backend/provider_portal/list_patients";
import { sendMessage as api_provider_portal_send_message_sendMessage } from "~backend/provider_portal/send_message";

export namespace provider_portal {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addNote = this.addNote.bind(this)
            this.getAuditLogs = this.getAuditLogs.bind(this)
            this.getMessages = this.getMessages.bind(this)
            this.getNotes = this.getNotes.bind(this)
            this.getPatientData = this.getPatientData.bind(this)
            this.grantAccess = this.grantAccess.bind(this)
            this.listPatients = this.listPatients.bind(this)
            this.sendMessage = this.sendMessage.bind(this)
        }

        public async addNote(params: RequestType<typeof api_provider_portal_add_note_addNote>): Promise<ResponseType<typeof api_provider_portal_add_note_addNote>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                content:            params.content,
                isVisibleToPatient: params.isVisibleToPatient,
                noteType:           params.noteType,
                priority:           params.priority,
                subject:            params.subject,
                token:              params.token,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/patients/${encodeURIComponent(params.patientUserId)}/notes`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_add_note_addNote>
        }

        public async getAuditLogs(params: RequestType<typeof api_provider_portal_get_audit_logs_getAuditLogs>): Promise<ResponseType<typeof api_provider_portal_get_audit_logs_getAuditLogs>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:         params.limit === undefined ? undefined : String(params.limit),
                patientUserId: params.patientUserId,
                token:         params.token,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/audit-logs`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_get_audit_logs_getAuditLogs>
        }

        public async getMessages(params: RequestType<typeof api_provider_portal_get_messages_getMessages>): Promise<ResponseType<typeof api_provider_portal_get_messages_getMessages>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                token: params.token,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/patients/${encodeURIComponent(params.patientUserId)}/messages`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_get_messages_getMessages>
        }

        public async getNotes(params: RequestType<typeof api_provider_portal_get_notes_getNotes>): Promise<ResponseType<typeof api_provider_portal_get_notes_getNotes>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                token: params.token,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/patients/${encodeURIComponent(params.patientUserId)}/notes`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_get_notes_getNotes>
        }

        public async getPatientData(params: RequestType<typeof api_provider_portal_get_patient_data_getPatientData>): Promise<ResponseType<typeof api_provider_portal_get_patient_data_getPatientData>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                token: params.token,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/patients/${encodeURIComponent(params.patientUserId)}/data`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_get_patient_data_getPatientData>
        }

        public async grantAccess(params: RequestType<typeof api_provider_portal_grant_access_grantAccess>): Promise<ResponseType<typeof api_provider_portal_grant_access_grantAccess>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                accessLevel: params.accessLevel,
                expiresAt:   params.expiresAt,
                token:       params.token,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/patients/${encodeURIComponent(params.patientUserId)}/access`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_grant_access_grantAccess>
        }

        public async listPatients(params: RequestType<typeof api_provider_portal_list_patients_listPatients>): Promise<ResponseType<typeof api_provider_portal_list_patients_listPatients>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                token: params.token,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/patients`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_list_patients_listPatients>
        }

        public async sendMessage(params: RequestType<typeof api_provider_portal_send_message_sendMessage>): Promise<ResponseType<typeof api_provider_portal_send_message_sendMessage>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                message: params.message,
                token:   params.token,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/provider/patients/${encodeURIComponent(params.patientUserId)}/messages`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_provider_portal_send_message_sendMessage>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { generateSpeech as api_voice_generate_speech_generateSpeech } from "~backend/voice/generate_speech";
import { listVoices as api_voice_list_voices_listVoices } from "~backend/voice/list_voices";

export namespace voice {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.generateSpeech = this.generateSpeech.bind(this)
            this.listVoices = this.listVoices.bind(this)
        }

        public async generateSpeech(params: RequestType<typeof api_voice_generate_speech_generateSpeech>): Promise<ResponseType<typeof api_voice_generate_speech_generateSpeech>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/voice/speak`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_voice_generate_speech_generateSpeech>
        }

        public async listVoices(): Promise<ResponseType<typeof api_voice_list_voices_listVoices>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/voice/list`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_voice_list_voices_listVoices>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    getMealLogs as api_wellness_diet_nutrition_getMealLogs,
    logMeal as api_wellness_diet_nutrition_logMeal
} from "~backend/wellness/diet_nutrition";
import { updateDietPreferences as api_wellness_diet_preferences_updateDietPreferences } from "~backend/wellness/diet_preferences";
import {
    createDoctorsOrder as api_wellness_doctors_orders_createDoctorsOrder,
    getActiveDoctorsOrders as api_wellness_doctors_orders_getActiveDoctorsOrders,
    logMedication as api_wellness_doctors_orders_logMedication
} from "~backend/wellness/doctors_orders";
import { downloadMealPlan as api_wellness_download_meal_plan_downloadMealPlan } from "~backend/wellness/download_meal_plan";
import { downloadShoppingList as api_wellness_download_shopping_list_downloadShoppingList } from "~backend/wellness/download_shopping_list";
import {
    getEveningRoutineLogs as api_wellness_evening_routine_getEveningRoutineLogs,
    logEveningRoutine as api_wellness_evening_routine_logEveningRoutine
} from "~backend/wellness/evening_routine";
import {
    analyzeFoodImage as api_wellness_food_images_analyzeFoodImage,
    getRefrigeratorScans as api_wellness_food_images_getRefrigeratorScans,
    uploadFoodImage as api_wellness_food_images_uploadFoodImage
} from "~backend/wellness/food_images";
import { generateMealPlan as api_wellness_generate_meal_plan_generateMealPlan } from "~backend/wellness/generate_meal_plan";
import { generateShoppingList as api_wellness_generate_shopping_list_generateShoppingList } from "~backend/wellness/generate_shopping_list";
import { getMealPlan as api_wellness_get_meal_plan_getMealPlan } from "~backend/wellness/get_meal_plan";
import {
    getMoodLogs as api_wellness_mood_getMoodLogs,
    logMood as api_wellness_mood_logMood
} from "~backend/wellness/mood";
import {
    getChatHistory as api_wellness_nutrition_chat_getChatHistory,
    nutritionChatStream as api_wellness_nutrition_chat_nutritionChatStream,
    sendChatMessage as api_wellness_nutrition_chat_sendChatMessage,
    startNutritionChat as api_wellness_nutrition_chat_startNutritionChat
} from "~backend/wellness/nutrition_chat";
import { nutritionChat as api_wellness_nutrition_chat_with_emma_nutritionChat } from "~backend/wellness/nutrition_chat_with_emma";
import { getNutritionPlan as api_wellness_nutrition_plan_getNutritionPlan } from "~backend/wellness/nutrition_plan";
import {
    getNutritionSetupProgress as api_wellness_nutrition_setup_progress_getNutritionSetupProgress,
    updateNutritionSetupProgress as api_wellness_nutrition_setup_progress_updateNutritionSetupProgress
} from "~backend/wellness/nutrition_setup_progress";
import {
    calculateDailyAchievement as api_wellness_nutrition_stats_calculateDailyAchievement,
    getNutritionStats as api_wellness_nutrition_stats_getNutritionStats
} from "~backend/wellness/nutrition_stats";
import {
    deleteSavedMealPlan as api_wellness_save_meal_plan_deleteSavedMealPlan,
    listSavedMealPlans as api_wellness_save_meal_plan_listSavedMealPlans,
    saveMealPlan as api_wellness_save_meal_plan_saveMealPlan,
    toggleFavoriteMealPlan as api_wellness_save_meal_plan_toggleFavoriteMealPlan
} from "~backend/wellness/save_meal_plan";
import { saveNutritionPlan as api_wellness_save_nutrition_plan_saveNutritionPlan } from "~backend/wellness/save_nutrition_plan";
import {
    deleteSavedShoppingList as api_wellness_save_shopping_list_deleteSavedShoppingList,
    listSavedShoppingLists as api_wellness_save_shopping_list_listSavedShoppingLists,
    saveShoppingList as api_wellness_save_shopping_list_saveShoppingList,
    toggleFavoriteShoppingList as api_wellness_save_shopping_list_toggleFavoriteShoppingList
} from "~backend/wellness/save_shopping_list";
import { updateMealPlan as api_wellness_update_meal_plan_updateMealPlan } from "~backend/wellness/update_meal_plan";

export namespace wellness {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.analyzeFoodImage = this.analyzeFoodImage.bind(this)
            this.calculateDailyAchievement = this.calculateDailyAchievement.bind(this)
            this.createDoctorsOrder = this.createDoctorsOrder.bind(this)
            this.deleteSavedMealPlan = this.deleteSavedMealPlan.bind(this)
            this.deleteSavedShoppingList = this.deleteSavedShoppingList.bind(this)
            this.downloadMealPlan = this.downloadMealPlan.bind(this)
            this.downloadShoppingList = this.downloadShoppingList.bind(this)
            this.generateMealPlan = this.generateMealPlan.bind(this)
            this.generateShoppingList = this.generateShoppingList.bind(this)
            this.getActiveDoctorsOrders = this.getActiveDoctorsOrders.bind(this)
            this.getChatHistory = this.getChatHistory.bind(this)
            this.getEveningRoutineLogs = this.getEveningRoutineLogs.bind(this)
            this.getMealLogs = this.getMealLogs.bind(this)
            this.getMealPlan = this.getMealPlan.bind(this)
            this.getMoodLogs = this.getMoodLogs.bind(this)
            this.getNutritionPlan = this.getNutritionPlan.bind(this)
            this.getNutritionSetupProgress = this.getNutritionSetupProgress.bind(this)
            this.getNutritionStats = this.getNutritionStats.bind(this)
            this.getRefrigeratorScans = this.getRefrigeratorScans.bind(this)
            this.listSavedMealPlans = this.listSavedMealPlans.bind(this)
            this.listSavedShoppingLists = this.listSavedShoppingLists.bind(this)
            this.logEveningRoutine = this.logEveningRoutine.bind(this)
            this.logMeal = this.logMeal.bind(this)
            this.logMedication = this.logMedication.bind(this)
            this.logMood = this.logMood.bind(this)
            this.nutritionChat = this.nutritionChat.bind(this)
            this.nutritionChatStream = this.nutritionChatStream.bind(this)
            this.saveMealPlan = this.saveMealPlan.bind(this)
            this.saveNutritionPlan = this.saveNutritionPlan.bind(this)
            this.saveShoppingList = this.saveShoppingList.bind(this)
            this.sendChatMessage = this.sendChatMessage.bind(this)
            this.startNutritionChat = this.startNutritionChat.bind(this)
            this.toggleFavoriteMealPlan = this.toggleFavoriteMealPlan.bind(this)
            this.toggleFavoriteShoppingList = this.toggleFavoriteShoppingList.bind(this)
            this.updateDietPreferences = this.updateDietPreferences.bind(this)
            this.updateMealPlan = this.updateMealPlan.bind(this)
            this.updateNutritionSetupProgress = this.updateNutritionSetupProgress.bind(this)
            this.uploadFoodImage = this.uploadFoodImage.bind(this)
        }

        public async analyzeFoodImage(params: RequestType<typeof api_wellness_food_images_analyzeFoodImage>): Promise<ResponseType<typeof api_wellness_food_images_analyzeFoodImage>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/food-image/analyze`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_food_images_analyzeFoodImage>
        }

        public async calculateDailyAchievement(params: RequestType<typeof api_wellness_nutrition_stats_calculateDailyAchievement>): Promise<ResponseType<typeof api_wellness_nutrition_stats_calculateDailyAchievement>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-achievement/calculate`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_stats_calculateDailyAchievement>
        }

        public async createDoctorsOrder(params: RequestType<typeof api_wellness_doctors_orders_createDoctorsOrder>): Promise<ResponseType<typeof api_wellness_doctors_orders_createDoctorsOrder>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/doctors-orders`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_doctors_orders_createDoctorsOrder>
        }

        public async deleteSavedMealPlan(params: RequestType<typeof api_wellness_save_meal_plan_deleteSavedMealPlan>): Promise<ResponseType<typeof api_wellness_save_meal_plan_deleteSavedMealPlan>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plans/saved/${encodeURIComponent(params.id)}`, {query, method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_meal_plan_deleteSavedMealPlan>
        }

        public async deleteSavedShoppingList(params: RequestType<typeof api_wellness_save_shopping_list_deleteSavedShoppingList>): Promise<ResponseType<typeof api_wellness_save_shopping_list_deleteSavedShoppingList>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/shopping-lists/saved/${encodeURIComponent(params.id)}`, {query, method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_shopping_list_deleteSavedShoppingList>
        }

        public async downloadMealPlan(params: RequestType<typeof api_wellness_download_meal_plan_downloadMealPlan>): Promise<ResponseType<typeof api_wellness_download_meal_plan_downloadMealPlan>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plans/download`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_download_meal_plan_downloadMealPlan>
        }

        public async downloadShoppingList(params: RequestType<typeof api_wellness_download_shopping_list_downloadShoppingList>): Promise<ResponseType<typeof api_wellness_download_shopping_list_downloadShoppingList>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/shopping-lists/download`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_download_shopping_list_downloadShoppingList>
        }

        public async generateMealPlan(params: RequestType<typeof api_wellness_generate_meal_plan_generateMealPlan>): Promise<ResponseType<typeof api_wellness_generate_meal_plan_generateMealPlan>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plan/generate`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_generate_meal_plan_generateMealPlan>
        }

        public async generateShoppingList(params: RequestType<typeof api_wellness_generate_shopping_list_generateShoppingList>): Promise<ResponseType<typeof api_wellness_generate_shopping_list_generateShoppingList>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plan/shopping-list`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_generate_shopping_list_generateShoppingList>
        }

        public async getActiveDoctorsOrders(params: RequestType<typeof api_wellness_doctors_orders_getActiveDoctorsOrders>): Promise<ResponseType<typeof api_wellness_doctors_orders_getActiveDoctorsOrders>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"] === undefined ? undefined : params["end_date"].toISOString(),
                limit:        params.limit === undefined ? undefined : String(params.limit),
                "start_date": params["start_date"] === undefined ? undefined : params["start_date"].toISOString(),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/doctors-orders/${encodeURIComponent(params.user_id)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_doctors_orders_getActiveDoctorsOrders>
        }

        public async getChatHistory(params: { session_id: string, user_id: string }): Promise<ResponseType<typeof api_wellness_nutrition_chat_getChatHistory>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-chat/${encodeURIComponent(params.session_id)}/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_chat_getChatHistory>
        }

        public async getEveningRoutineLogs(params: RequestType<typeof api_wellness_evening_routine_getEveningRoutineLogs>): Promise<ResponseType<typeof api_wellness_evening_routine_getEveningRoutineLogs>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"] === undefined ? undefined : params["end_date"].toISOString(),
                limit:        params.limit === undefined ? undefined : String(params.limit),
                "start_date": params["start_date"] === undefined ? undefined : params["start_date"].toISOString(),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/evening-routine/${encodeURIComponent(params.user_id)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_evening_routine_getEveningRoutineLogs>
        }

        public async getMealLogs(params: RequestType<typeof api_wellness_diet_nutrition_getMealLogs>): Promise<ResponseType<typeof api_wellness_diet_nutrition_getMealLogs>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"] === undefined ? undefined : params["end_date"].toISOString(),
                limit:        params.limit === undefined ? undefined : String(params.limit),
                "start_date": params["start_date"] === undefined ? undefined : params["start_date"].toISOString(),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meals/${encodeURIComponent(params.user_id)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_diet_nutrition_getMealLogs>
        }

        public async getMealPlan(params: RequestType<typeof api_wellness_get_meal_plan_getMealPlan>): Promise<ResponseType<typeof api_wellness_get_meal_plan_getMealPlan>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plan/get`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_get_meal_plan_getMealPlan>
        }

        public async getMoodLogs(params: RequestType<typeof api_wellness_mood_getMoodLogs>): Promise<ResponseType<typeof api_wellness_mood_getMoodLogs>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"] === undefined ? undefined : params["end_date"].toISOString(),
                limit:        params.limit === undefined ? undefined : String(params.limit),
                "start_date": params["start_date"] === undefined ? undefined : params["start_date"].toISOString(),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/moods/${encodeURIComponent(params.user_id)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_mood_getMoodLogs>
        }

        public async getNutritionPlan(params: { user_id: string }): Promise<ResponseType<typeof api_wellness_nutrition_plan_getNutritionPlan>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-plan/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_plan_getNutritionPlan>
        }

        public async getNutritionSetupProgress(params: { user_id: string }): Promise<ResponseType<typeof api_wellness_nutrition_setup_progress_getNutritionSetupProgress>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-setup/progress/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_setup_progress_getNutritionSetupProgress>
        }

        public async getNutritionStats(params: { user_id: string }): Promise<ResponseType<typeof api_wellness_nutrition_stats_getNutritionStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-stats/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_stats_getNutritionStats>
        }

        public async getRefrigeratorScans(params: RequestType<typeof api_wellness_food_images_getRefrigeratorScans>): Promise<ResponseType<typeof api_wellness_food_images_getRefrigeratorScans>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit: params.limit === undefined ? undefined : String(params.limit),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/refrigerator-scans/${encodeURIComponent(params.user_id)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_food_images_getRefrigeratorScans>
        }

        public async listSavedMealPlans(params: RequestType<typeof api_wellness_save_meal_plan_listSavedMealPlans>): Promise<ResponseType<typeof api_wellness_save_meal_plan_listSavedMealPlans>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plans/saved`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_meal_plan_listSavedMealPlans>
        }

        public async listSavedShoppingLists(params: RequestType<typeof api_wellness_save_shopping_list_listSavedShoppingLists>): Promise<ResponseType<typeof api_wellness_save_shopping_list_listSavedShoppingLists>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/shopping-lists/saved`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_shopping_list_listSavedShoppingLists>
        }

        public async logEveningRoutine(params: RequestType<typeof api_wellness_evening_routine_logEveningRoutine>): Promise<ResponseType<typeof api_wellness_evening_routine_logEveningRoutine>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/evening-routine`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_evening_routine_logEveningRoutine>
        }

        public async logMeal(params: RequestType<typeof api_wellness_diet_nutrition_logMeal>): Promise<ResponseType<typeof api_wellness_diet_nutrition_logMeal>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_diet_nutrition_logMeal>
        }

        public async logMedication(params: RequestType<typeof api_wellness_doctors_orders_logMedication>): Promise<ResponseType<typeof api_wellness_doctors_orders_logMedication>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/medication-log`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_doctors_orders_logMedication>
        }

        public async logMood(params: RequestType<typeof api_wellness_mood_logMood>): Promise<ResponseType<typeof api_wellness_mood_logMood>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/mood`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_mood_logMood>
        }

        public async nutritionChat(params: RequestType<typeof api_wellness_nutrition_chat_with_emma_nutritionChat>): Promise<ResponseType<typeof api_wellness_nutrition_chat_with_emma_nutritionChat>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-chat`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_chat_with_emma_nutritionChat>
        }

        public async nutritionChatStream(): Promise<StreamInOut<StreamRequest<typeof api_wellness_nutrition_chat_nutritionChatStream>, StreamResponse<typeof api_wellness_nutrition_chat_nutritionChatStream>>> {
            return await this.baseClient.createStreamInOut(`/wellness/nutrition-chat/stream`)
        }

        public async saveMealPlan(params: RequestType<typeof api_wellness_save_meal_plan_saveMealPlan>): Promise<ResponseType<typeof api_wellness_save_meal_plan_saveMealPlan>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plans/save`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_meal_plan_saveMealPlan>
        }

        public async saveNutritionPlan(params: RequestType<typeof api_wellness_save_nutrition_plan_saveNutritionPlan>): Promise<ResponseType<typeof api_wellness_save_nutrition_plan_saveNutritionPlan>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-plan/save`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_nutrition_plan_saveNutritionPlan>
        }

        public async saveShoppingList(params: RequestType<typeof api_wellness_save_shopping_list_saveShoppingList>): Promise<ResponseType<typeof api_wellness_save_shopping_list_saveShoppingList>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/shopping-lists/save`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_shopping_list_saveShoppingList>
        }

        public async sendChatMessage(params: RequestType<typeof api_wellness_nutrition_chat_sendChatMessage>): Promise<ResponseType<typeof api_wellness_nutrition_chat_sendChatMessage>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-chat/message`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_chat_sendChatMessage>
        }

        public async startNutritionChat(params: RequestType<typeof api_wellness_nutrition_chat_startNutritionChat>): Promise<ResponseType<typeof api_wellness_nutrition_chat_startNutritionChat>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-chat/start`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_chat_startNutritionChat>
        }

        public async toggleFavoriteMealPlan(params: RequestType<typeof api_wellness_save_meal_plan_toggleFavoriteMealPlan>): Promise<ResponseType<typeof api_wellness_save_meal_plan_toggleFavoriteMealPlan>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                isFavorite: params.isFavorite,
                "user_id":  params["user_id"],
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plans/${encodeURIComponent(params.id)}/favorite`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_meal_plan_toggleFavoriteMealPlan>
        }

        public async toggleFavoriteShoppingList(params: RequestType<typeof api_wellness_save_shopping_list_toggleFavoriteShoppingList>): Promise<ResponseType<typeof api_wellness_save_shopping_list_toggleFavoriteShoppingList>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                isFavorite: params.isFavorite,
                "user_id":  params["user_id"],
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/shopping-lists/${encodeURIComponent(params.id)}/favorite`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_save_shopping_list_toggleFavoriteShoppingList>
        }

        public async updateDietPreferences(params: RequestType<typeof api_wellness_diet_preferences_updateDietPreferences>): Promise<ResponseType<typeof api_wellness_diet_preferences_updateDietPreferences>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/diet-preferences`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_diet_preferences_updateDietPreferences>
        }

        public async updateMealPlan(params: RequestType<typeof api_wellness_update_meal_plan_updateMealPlan>): Promise<ResponseType<typeof api_wellness_update_meal_plan_updateMealPlan>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/meal-plan/update`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_update_meal_plan_updateMealPlan>
        }

        public async updateNutritionSetupProgress(params: RequestType<typeof api_wellness_nutrition_setup_progress_updateNutritionSetupProgress>): Promise<ResponseType<typeof api_wellness_nutrition_setup_progress_updateNutritionSetupProgress>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                "current_step":    params["current_step"],
                "is_completed":    params["is_completed"],
                "steps_completed": params["steps_completed"],
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/nutrition-setup/progress/${encodeURIComponent(params.user_id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_nutrition_setup_progress_updateNutritionSetupProgress>
        }

        public async uploadFoodImage(params: RequestType<typeof api_wellness_food_images_uploadFoodImage>): Promise<ResponseType<typeof api_wellness_food_images_uploadFoodImage>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness/food-image/upload`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_food_images_uploadFoodImage>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    addFromConversation as api_wellness_journal_add_manual_addFromConversation,
    addManualEntry as api_wellness_journal_add_manual_addManualEntry
} from "~backend/wellness_journal/add_manual";
import { analyzeTrends as api_wellness_journal_analyze_trends_analyzeTrends } from "~backend/wellness_journal/analyze_trends";
import { createChapter as api_wellness_journal_create_chapter_createChapter } from "~backend/wellness_journal/create_chapter";
import { createSection as api_wellness_journal_create_section_createSection } from "~backend/wellness_journal/create_section";
import { generateChapterInsights as api_wellness_journal_generate_chapter_insights_generateChapterInsights } from "~backend/wellness_journal/generate_chapter_insights";
import { generateDailySummary as api_wellness_journal_generate_summary_generateDailySummary } from "~backend/wellness_journal/generate_summary";
import { getChapterDetails as api_wellness_journal_get_chapter_details_getChapterDetails } from "~backend/wellness_journal/get_chapter_details";
import { getChapters as api_wellness_journal_get_chapters_getChapters } from "~backend/wellness_journal/get_chapters";
import {
    getJournalEntries as api_wellness_journal_get_entries_getJournalEntries,
    getJournalStats as api_wellness_journal_get_entries_getJournalStats
} from "~backend/wellness_journal/get_entries";
import { linkEntryToChapter as api_wellness_journal_link_entry_to_chapter_linkEntryToChapter } from "~backend/wellness_journal/link_entry_to_chapter";
import { logSectionCompletion as api_wellness_journal_log_section_completion_logSectionCompletion } from "~backend/wellness_journal/log_section_completion";
import { updateChapter as api_wellness_journal_update_chapter_updateChapter } from "~backend/wellness_journal/update_chapter";
import { updateSection as api_wellness_journal_update_section_updateSection } from "~backend/wellness_journal/update_section";

export namespace wellness_journal {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addFromConversation = this.addFromConversation.bind(this)
            this.addManualEntry = this.addManualEntry.bind(this)
            this.analyzeTrends = this.analyzeTrends.bind(this)
            this.createChapter = this.createChapter.bind(this)
            this.createSection = this.createSection.bind(this)
            this.generateChapterInsights = this.generateChapterInsights.bind(this)
            this.generateDailySummary = this.generateDailySummary.bind(this)
            this.getChapterDetails = this.getChapterDetails.bind(this)
            this.getChapters = this.getChapters.bind(this)
            this.getJournalEntries = this.getJournalEntries.bind(this)
            this.getJournalStats = this.getJournalStats.bind(this)
            this.linkEntryToChapter = this.linkEntryToChapter.bind(this)
            this.logSectionCompletion = this.logSectionCompletion.bind(this)
            this.updateChapter = this.updateChapter.bind(this)
            this.updateSection = this.updateSection.bind(this)
        }

        public async addFromConversation(params: RequestType<typeof api_wellness_journal_add_manual_addFromConversation>): Promise<ResponseType<typeof api_wellness_journal_add_manual_addFromConversation>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/add-from-conversation`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_add_manual_addFromConversation>
        }

        public async addManualEntry(params: RequestType<typeof api_wellness_journal_add_manual_addManualEntry>): Promise<ResponseType<typeof api_wellness_journal_add_manual_addManualEntry>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/add-manual`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_add_manual_addManualEntry>
        }

        public async analyzeTrends(params: RequestType<typeof api_wellness_journal_analyze_trends_analyzeTrends>): Promise<ResponseType<typeof api_wellness_journal_analyze_trends_analyzeTrends>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/analyze`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_analyze_trends_analyzeTrends>
        }

        public async createChapter(params: RequestType<typeof api_wellness_journal_create_chapter_createChapter>): Promise<ResponseType<typeof api_wellness_journal_create_chapter_createChapter>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/chapters/create`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_create_chapter_createChapter>
        }

        public async createSection(params: RequestType<typeof api_wellness_journal_create_section_createSection>): Promise<ResponseType<typeof api_wellness_journal_create_section_createSection>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/sections/create`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_create_section_createSection>
        }

        public async generateChapterInsights(params: RequestType<typeof api_wellness_journal_generate_chapter_insights_generateChapterInsights>): Promise<ResponseType<typeof api_wellness_journal_generate_chapter_insights_generateChapterInsights>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/chapters/insights`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_generate_chapter_insights_generateChapterInsights>
        }

        public async generateDailySummary(params: RequestType<typeof api_wellness_journal_generate_summary_generateDailySummary>): Promise<ResponseType<typeof api_wellness_journal_generate_summary_generateDailySummary>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/generate-daily-summary`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_generate_summary_generateDailySummary>
        }

        public async getChapterDetails(params: RequestType<typeof api_wellness_journal_get_chapter_details_getChapterDetails>): Promise<ResponseType<typeof api_wellness_journal_get_chapter_details_getChapterDetails>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/chapters/details`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_get_chapter_details_getChapterDetails>
        }

        public async getChapters(params: RequestType<typeof api_wellness_journal_get_chapters_getChapters>): Promise<ResponseType<typeof api_wellness_journal_get_chapters_getChapters>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/chapters/list`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_get_chapters_getChapters>
        }

        public async getJournalEntries(params: RequestType<typeof api_wellness_journal_get_entries_getJournalEntries>): Promise<ResponseType<typeof api_wellness_journal_get_entries_getJournalEntries>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/entries`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_get_entries_getJournalEntries>
        }

        public async getJournalStats(params: RequestType<typeof api_wellness_journal_get_entries_getJournalStats>): Promise<ResponseType<typeof api_wellness_journal_get_entries_getJournalStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/stats`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_get_entries_getJournalStats>
        }

        public async linkEntryToChapter(params: RequestType<typeof api_wellness_journal_link_entry_to_chapter_linkEntryToChapter>): Promise<ResponseType<typeof api_wellness_journal_link_entry_to_chapter_linkEntryToChapter>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/entries/link`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_link_entry_to_chapter_linkEntryToChapter>
        }

        public async logSectionCompletion(params: RequestType<typeof api_wellness_journal_log_section_completion_logSectionCompletion>): Promise<ResponseType<typeof api_wellness_journal_log_section_completion_logSectionCompletion>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/sections/log`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_log_section_completion_logSectionCompletion>
        }

        public async updateChapter(params: RequestType<typeof api_wellness_journal_update_chapter_updateChapter>): Promise<ResponseType<typeof api_wellness_journal_update_chapter_updateChapter>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/chapters/update`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_update_chapter_updateChapter>
        }

        public async updateSection(params: RequestType<typeof api_wellness_journal_update_section_updateSection>): Promise<ResponseType<typeof api_wellness_journal_update_section_updateSection>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/wellness_journal/sections/update`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_wellness_journal_update_section_updateSection>
        }
    }
}


type PickMethods<Type> = Omit<CallParameters, "method"> & { method?: Type };

// Helper type to omit all fields that are cookies.
type OmitCookie<T> = {
  [K in keyof T as T[K] extends CookieWithOptions<any> ? never : K]: T[K];
};

type RequestType<Type extends (...args: any[]) => any> =
  Parameters<Type> extends [infer H, ...any[]]
    ? OmitCookie<H>
    : void;

type ResponseType<Type extends (...args: any[]) => any> = OmitCookie<Awaited<ReturnType<Type>>>;

function dateReviver(key: string, value: any): any {
  if (
    typeof value === "string" &&
    value.length >= 10 &&
    value.charCodeAt(0) >= 48 && // '0'
    value.charCodeAt(0) <= 57 // '9'
  ) {
    const parsedDate = new Date(value);
    if (!isNaN(parsedDate.getTime())) {
      return parsedDate;
    }
  }
  return value;
}


function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

import {
  StreamInOutHandlerFn,
  StreamInHandlerFn,
  StreamOutHandlerFn,
} from "encore.dev/api";

type StreamRequest<Type> = Type extends
  | StreamInOutHandlerFn<any, infer Req, any>
  | StreamInHandlerFn<any, infer Req, any>
  | StreamOutHandlerFn<any, any>
  ? Req
  : never;

type StreamResponse<Type> = Type extends
  | StreamInOutHandlerFn<any, any, infer Resp>
  | StreamInHandlerFn<any, any, infer Resp>
  | StreamOutHandlerFn<any, infer Resp>
  ? Resp
  : never;


function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data, dateReviver))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
  | RequestType<typeof auth_auth>
  | Promise<RequestType<typeof auth_auth> | undefined>
  | undefined;

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
    readonly authGenerator?: AuthDataGenerator

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "-Generated-TS-Client (Encore/1.50.6)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }

        // Setup an authentication data generator using the auth data token option
        if (options.auth !== undefined) {
            const auth = options.auth
            if (typeof auth === "function") {
                this.authGenerator = auth
            } else {
                this.authGenerator = () => auth
            }
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        let authData: RequestType<typeof auth_auth> | undefined;

        // If authorization data generator is present, call it and add the returned data to the request
        if (this.authGenerator) {
            const mayBePromise = this.authGenerator();
            if (mayBePromise instanceof Promise) {
                authData = await mayBePromise;
            } else {
                authData = mayBePromise;
            }
        }

        if (authData) {
            const data: CallParameters = {};

            data.headers = makeRecord<string, string>({
                authorization: authData.authorization,
            });

            return data;
        }

        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(path: string, params?: CallParameters): Promise<Response> {
        return this.callAPI(path, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(path: string, params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}

export default new Client(import.meta.env.VITE_CLIENT_TARGET, { requestInit: { credentials: "include" } });
